- name: Get Secrtes from Vault
  hosts: localhost
  connection: local
  tasks:
    - name: print
      ansible.builtin.debug:
        msg: "{{ lookup('community.hashi_vault.hashi_vault', 'secret=test/demo:username token={{vault_token}} url=http://vault-internal.gvndevops12.tech:8200') }}"
